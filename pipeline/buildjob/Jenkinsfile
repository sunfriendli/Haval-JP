#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Find Latest Ubuntu') {
            steps {
                script {
                    println env.WORKSPACE
                    getNextBuildJob("config.yml", "base", [])
                }
            }
        }
    }
}